// math.cpp — implementations of the functions declared in math.h
// The #include "math.h" lets the compiler verify that signatures match.

#include "math.h"

int add(int x, int y)      { return x + y; }
int subtract(int x, int y) { return x - y; }
int multiply(int x, int y) { return x * y; }
int divide(int x, int y)   { return x / y; } // integer division; no remainder
