#include <iostream>

using namespace std;

int main() {
    cout<<"Hello"<<"\n";
    return 0;
}

/*
So basicly when we specify a library we simple to all the function 
of that library to the global scope. this i suppose is not good and possibly not efficient
and safe because i might cause nameing conflicts
*/